# OpenCTI configuration
# The OpenCTI platform uses a Docker image that pulls its configuration from environment variables.
# For a minimal setup, we expose the default port and set the database credentials.
# The database will be a separate PostgreSQL container.
